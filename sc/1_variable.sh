#!/bin/sh
#the script tests how we can use variables defined by users
# we are defining the variables
name=john
company=Tesla

# we want to call the variables
echo "my username is $name"
echo "i work with $company as a devops engineer"
